# `+` composes with ggplot components and returns a plain ggplot, so `bp |> gg_biplot() + labs(...)` still works.

`+` composes with ggplot components and returns a plain ggplot, so
`bp |> gg_biplot() + labs(...)` still works.

## Usage

``` r
# S3 method for class 'gg_biplot'
e1 + e2
```
